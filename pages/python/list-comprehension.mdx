# Python List Comprehension

List comprehension is a simple, declarative technique for generating a new list from an existing list.

```python
numbers = [2, 4, 6, 8, 10]
squares = [num**2 for num in numbers]
print(f'result = {squares}')

# result = [4, 16, 36, 64, 100]
```

